interface:
  display_name: "Codex"
  short_description: "Coordinate a current Codex CLI session"
  default_prompt: "Use $codex to run Codex CLI only after checking the installed command help, preserving the user's configured model unless they explicitly choose another, and establishing the requested permission boundary."
