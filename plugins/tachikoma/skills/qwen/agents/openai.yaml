interface:
  display_name: "Qwen"
  short_description: "Coordinate a current Qwen Code session"
  default_prompt: "Use $qwen to run Qwen Code only after checking the installed command help, preserving the user's configured model unless they explicitly choose another, and establishing the requested permission boundary."
