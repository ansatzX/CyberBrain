interface:
  display_name: "Codex Compatible"
  short_description: "Prefix rule escalation and sandbox writable roots"
  default_prompt: "Use $codex-compatible when the task touches exec_command escalation, prefix_rule, or sandbox permissions: identify the reusable command prefix, check existing approved rules, and verify writable roots coverage."
